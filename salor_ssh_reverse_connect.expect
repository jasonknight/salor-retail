#!/usr/bin/expect

set timeout 30
set pass [lindex $argv 0]
spawn ssh -f -N -p 26 -R 10000:localhost:26 gitrepos@demo.salorpos.com

expect {
"(yes/no)?" { send "yes\n" }
"password: " { send "$pass\n" }
}

expect {
"(yes/no)?" { send "yes\n" }
"password: " { send "$pass\n" }
}

#expect {
#"hi" { send "hello" }
#"pass" { send "$pass\n" }
#}
#
#expect {
#"hi" { send "hello" }
#"pass" { send "$pass\n" }
#}
