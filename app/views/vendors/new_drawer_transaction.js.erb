updateDrawer({amount:'<%= GlobalData.salor_user.drawer.amount %>'});
if (typeof SalorPrinter != 'undefined') {
  print_url(Conf.url + '/vendors/render_drawer_transaction_receipt?id=<%= @drawer_transaction.id %>');
  quick_open_drawer();
}
<%= render :partial => "shared/js/global_errors_js" %>
