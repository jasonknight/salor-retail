<%
  if AppConfig.standalone then
%>
  <% if $User.is_technician? %>
    <%= salor_render :partial => "shared/technician_control_panel" %>
  <% else %>    
    <%= salor_render :partial => "shared/login" %>
  <% end -%>
<% else %>
<%= salor_render :partial => "shared/front_page" %>
<% end %>
