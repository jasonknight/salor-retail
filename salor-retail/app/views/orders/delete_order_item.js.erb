//CustomerScreen.refresh("http://localhost:3000/orders/<%= @order.id %>/customer_display",800,480);
updateOrder(<%= raw @order.to_json %>);
<%
  items = []
  @order.order_items.reload.visible.each do |oi|
    items << oi.to_json
  end
%>
updateOrderItems([<%= raw items.join(',') %>]);
updateCustomerView(false, <%= @order.id %>);
Order = <%= raw @order.to_json %>;
$(".complete-order-total").html($('#pos_order_total').html());
Order = <%= raw @order.to_json %>;
focusInput($('#keyboard_input'));
