Order = <%= raw @order.to_json %>;
updateOrder(<%= raw @order.to_json %>);
<%
  items = []
  @order.order_items.visible.each do |oi|
    items << oi.to_json
  end
  
  if items.any? then
%>

updateOrderItems( [<%= raw items.join(",") %>]);

<% else %>

$('.pos-table-left-column-items').html('');

<% end -%>

<% if @inst.class == OrderItem and @inst.item.behavior == 'gift_card' and @inst.item.activated.nil? %>
     print_url(Register.sticker_printer, '/items/labels', '&id=<%= @inst.item.id %>&type=sticker&style=default')
<% end %>