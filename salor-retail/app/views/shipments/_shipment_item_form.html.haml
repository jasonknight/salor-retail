:javascript
  $(function() {
    ShipmentItems = #{ raw Shipment.shipment_items_to_json(@shipment_items) };
    Shipment = #{ raw @shipment.to_json };
    shipments.updateLineItems();
    shipments.updateShipment();

    $('#main_shipment_sku_field').keydown(function(e) {
      if (e.which == 13) {
        shipments.submitLineItem($('#main_shipment_sku_field').val());
        $('#main_shipment_sku_field').val('');
        return false;
      }
    });
  sr.fn.onscreen_keyboard.make($('#main_shipment_sku_field'));

  });

#pos_order_total.pos-order-total
%input#main_shipment_sku_field

  
  
#shipment_items_container
