= raw content_box_top(I18n.t("headings.database_distiller"))

%p== #{ t('.used_item_ids') }: #{ @used_item_ids.size }
%p== #{ t('.relevant_item_ids') }: #{ @relevant_item_ids.size }
%p== #{ t('.unused_item_ids') }: #{ @unused_item_ids.size }
%p== #{ t('.hidden') }: #{ @hidden.size }
%p== #{ t('.hidden_by_distiller') }: #{ @hidden_by_distiller.size }

%p= link_to t('.delete_unused_items'), '/items/distill_database', :confirm => "#{ t('.are_you_sure') }"
