<%
if params[:sku] then
  @item = Item.find_by_sku(params[:sku])
  if @item then
    @item.update_attribute(:location_id, params[:location_id])
  end
end
%>
<%= render :partial => "shared/js/global_errors_js" %>
